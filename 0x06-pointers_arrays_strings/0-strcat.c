#include "holberton.h"
/**
*_strca - concatenates two strings
*@dest: pointer to char
*@src: pointer to char
*/
char *_strcat(char *dest, char *src)
{
int a = 0;
int b = 0;
while (*(dest + a))
a++;
while ((*(dest + a) = *(src + a)))
{
b++;
b++;
}
return (dest);
}
